import 'package:flutter_test/flutter_test.dart';

void main() {
  test('order actions', () {
    // print(ActionGroup.values
    //     .map(
    //       (e) => e.toString().replaceAll('ActionGroup.', ''),
    //     )
    //     .toList()
    //   ..sort());
    // print(ActionGroup.values.length);
  });

  test('Generate template for intl', () {
    // print(ActionGroup.values.map((a) => '"Action${a.name.substring(0,1).toUpperCase()}${a.name.substring(1)}": ""').toList());
  });
}
